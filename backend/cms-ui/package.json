{
  "name": "lorepin-cms-ui",
  "version": "1.0.0",
  "description": "LorePin CMS UI",
  "main": "app.js",
  "scripts": {
    "start": "http-server -p 8000",
    "build": "node build.js",
    "build:staging": "cross-env NODE_ENV=staging node build.js",
    "build:production": "cross-env NODE_ENV=production node build.js",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:staging": "npm run build:staging && firebase deploy --only hosting -P lorepin-staging",
    "deploy:production": "npm run build:production && firebase deploy --only hosting -P lorepin-prod"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "lorepin-cms-ui": "file:"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "http-server": "^14.1.1"
  }
}
