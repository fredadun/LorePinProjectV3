// Import the environment configuration
import { firebaseConfig, shouldUseEmulators, emulatorConfig } from './environment';

// Export the Firebase configuration
export { firebaseConfig };

// Helper function to determine if we should use emulators
export { shouldUseEmulators };

// Emulator configuration
export { emulatorConfig }; 