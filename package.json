{
  "name": "lorepin-project",
  "version": "1.0.0",
  "description": "LorePin is a location-based challenge platform that connects users with sponsors through engaging challenges and rewards.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests specified yet\" && exit 0",
    "build": "echo \"No build step required at root level\" && exit 0",
    "lint": "echo \"No lint step required at root level\" && exit 0",
    "type-check": "echo \"No type-check step required at root level\" && exit 0",
    "install:all": "npm install && cd frontend && npm install && cd ../backend/functions && npm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fredadun/LorePinProjectV3.git"
  },
  "keywords": [
    "lorepin",
    "challenges",
    "location-based",
    "firebase"
  ],
  "author": "LorePin Team",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/fredadun/LorePinProjectV3/issues"
  },
  "homepage": "https://github.com/fredadun/LorePinProjectV3#readme",
  "dependencies": {
    "firebase": "^10.8.0",
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^4.5.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}